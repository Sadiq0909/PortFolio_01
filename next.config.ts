import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['assets.aceternity.com' , 'images.unsplash.com' ,'encrypted-tbn0.gstatic.com', 'other-allowed-domains.com'],
  },
};

export default nextConfig;
